# Optional — enables email notifications for new contact form leads via
# Resend (https://resend.com). Leave unset and the form still works fully;
# leads are saved to data/leads.json regardless of these being set.
RESEND_API_KEY=
CONTACT_TO_EMAIL=care@sktermitesolutions.com
CONTACT_FROM_EMAIL=onboarding@resend.dev
